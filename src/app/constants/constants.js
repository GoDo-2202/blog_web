export const CONSTANTS = {
    AVATAR: "avatar",
    IMAGES: "images",
    USER_PRIVATE_FIELDS: "+password +refreshTokenHash",
    USER_AUTH_FIELDS: "+password",
    USER_REFRESH_FIELDS: "+refreshTokenHash",
}

